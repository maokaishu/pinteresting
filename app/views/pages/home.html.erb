<div class="jumbotron center">
	<h1 class="welcome">Hungry?</h1>

<% if user_signed_in? %>
# do something
<% else %>
	<p>
		<%= link_to "Log in", new_user_session_path, class: "btn btn-default bin-lg" %>
	 <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary bin-lg" %>
	 	</p>

	 	<% end %>
</div>
